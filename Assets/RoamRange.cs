using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RoamRange : MonoBehaviour
{
    public static RectTransform roamRange;
    // Start is called before the first frame update
    void Start()
    {
        roamRange=GetComponent<RectTransform>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
